- builder:
    name: scratch-build-rpm
    builders:
      - shell: |
          #!/bin/bash -e
          # Ensure we do not expose secrets
          set +x
          set -o pipefail
          cbs build --wait --scratch {buildtarget} *.src.rpm
          rm *.src.rpm

