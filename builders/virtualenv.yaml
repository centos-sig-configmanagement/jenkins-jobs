- builder:
    name: setup-virtualenv
    builders:
      - shell: |
          #!/bin/bash -xe
          virtualenv venv
          source venv/bin/activate
          # Workaround for https://github.com/openaps/openaps/issues/95
          pip install --upgrade setuptools
          pip install -r requirements.txt
